import React from "react";
import styled from "styled-components";

const Add = () => {
  return (
    <Wrapper>
      <h2 className="title">Add Gig</h2>
    </Wrapper>
  );
};

const Wrapper = styled.div`
  .title {
    color: slateblue;
  }
`;

export default Add;
